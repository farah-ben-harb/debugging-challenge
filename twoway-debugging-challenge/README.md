# Trading Dashboard Debugging Challenge

Intentionally buggy full-stack Node.js + TypeScript project.

Start with:

```bash
npm install
npm test
npm run typecheck
npm run dev
```

Then open http://localhost:3000
